---
type: guide
title: 使用说明
audience: reader
edition: 12-episode-sample
---

# 使用说明

这个 Vault 既是 12 期 Lenny's Podcast 的知识库 Sample，也是一套可以直接 fork 的 LLM Wiki 模板。核心不是“把每份材料总结一次”，而是让新材料持续更新同一张知识网络。

## 一分钟打开

1. 安装 [Obsidian](https://obsidian.md/)；
2. 下载或 clone 整个仓库；
3. 在 Obsidian 中选择 **Open folder as vault**；
4. 打开 [[首页]]；
5. 从 `3.0 Maps/`、`2.0 Topics/`、`1.0 Material/` 或 [[目录]] 开始阅读。

不安装 Obsidian 也能直接阅读 Markdown，但 wikilink、backlinks 和 local graph 会缺失。

## 目录结构

```text
0.0 Inbox/       原始材料投递区；Agent 只读，不改写
1.0 Material/    所有单一来源的编译页，不按材料类型拆文件夹
2.0 Topics/      跨来源知识页，使用 canonical 名称，全部平铺
3.0 Maps/        按领域组织的导航与阅读路径
目录.md      全库页面索引与一句话说明
.agents/skills/  仓库级导入 Skill、质量规范与导入日志
AGENTS.md     所有 Agent 共用的仓库规则
```

`0.0 Inbox/` 里的原始材料与 `1.0 Material/` 里的来源页不是一回事：前者是证据，后者是可读、可链接的编译结果。默认 `.gitignore` 不追踪你放进 `0.0 Inbox/` 的内容，避免误公开私人或受版权保护的材料。

## 四类页面如何协作

### 1.0 Material：保留单一来源的逻辑

来源页回答“这份材料到底说了什么”，保留作者判断、推理、例子、反例、边界和可核验定位。播客、访谈、文章、论文、书摘、报告和会议记录全部进入 `1.0 Material/`，再通过 frontmatter 标记材料类型。

来源页没有统一正文模板。Agent 应根据材料本身的结构决定章节：论文可能围绕研究问题、方法、结果与限制；访谈可能围绕问题链；随笔可能围绕论点与论证。统一的是证据、归因、定位和链接标准，不是文章长相。

### 2.0 Topics：跨来源综合

Topic 回答“关于这个问题，现有材料合起来告诉我们什么”。它必须区分共识、分歧、成立条件与常见误用，不能只是复制多个来源摘要。

### 3.0 Maps：导航，不复制正文

Map 围绕一个稳定领域组织关键问题、Topic 和阅读顺序。同一 Topic 可以出现在多张 Map 中。

### 目录与导入日志：让库可维护

- [[目录]] 帮助人和 Agent 先查已有页面，再决定更新还是新建；
- Skill 内部的导入日志记录材料名、相对路径、指纹、导入结果和异常，避免重复导入。它也是唯一的处理状态，不需要再维护一份 manifest。

## 加入一份新材料

1. 把 `.md`、`.txt` 或可读取的文档放进 `0.0 Inbox/`。网页材料建议保存正文并同时记录原始 URL；PDF 要保留页码。
2. 用 Codex 打开这个仓库的根目录。
3. 直接说：`帮我处理 Inbox 里最新加入的材料。`
4. Codex 会根据请求自动调用仓库里的 `llm-wiki-ingest` Skill；需要显式指定时说：`使用 $llm-wiki-ingest 处理 Inbox 里的新增材料。`
5. 完成后检查 Agent 报告中的来源异常、弱证据、冲突与人工复核项。

Skill 会比较 Inbox 文件的相对路径和 SHA-256 与导入日志：未登记的文件或指纹发生变化的文件才会被处理。文件插入或修改时间只用于排序，避免仅靠时间误判。若你明确点名某个文件，Agent 只处理该文件。

材料没有作者、日期、页码或时间戳时，应明确标成缺失或弱定位，不能让 Agent 猜。短材料不必硬凑 15 个洞见，长材料也不能为了省 Token 压成 4 条 takeaway。

## 推荐的提问方式

提问时可以直接要求 Agent：

```text
请先查阅《目录.md》，再读取与问题最相关的来源页和 Topic 回答。
区分来源事实、不同作者观点和你的跨来源推断，并在答案末尾列出使用的 Wiki 页面。
若证据不足或来源冲突，请明确说明，不要补全。
除非我明确同意，不要把这次问答自动写回知识库。
```

## 维护知识库

定期让 Agent 执行以下检查：

- 失效的 `[[双向链接]]`；
- 没有入链或未被目录收录的孤立页面；
- 重复或高度相似的 Topic；
- 同一概念的命名、aliases、`concept_id` 漂移；
- 来源页与 Topic 之间的矛盾或错误归因；
- `目录.md`、Skill 内部导入日志和实际文件不一致。

修复机械问题可以直接完成；涉及内容合并、删除或改变作者原意时，先给建议，不要擅自处理。

## Sample 与全量版

本仓库只提供 12 期 Lenny's Podcast 的派生知识页。未来 300 多期全量索引会在独立项目中继续构建；你下载后的个人副本则可以吸收任何你有权使用的文字材料。
